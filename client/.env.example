# Copy this file to .env.local for local development
# For Vercel deployment, set this as an environment variable in your project settings

# Local dev (server running on port 5500)
VITE_API_URL=http://localhost:5500/api

# Production (when deployed on Vercel — API is served from same domain)
# VITE_API_URL=/api
